@model IEnumerable<D_real_social_app.Models.Connection>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserID2)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserID2)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ConnectionID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ConnectionID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ConnectionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
